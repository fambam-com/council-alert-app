// import { AlertRemark } from "./Chain";

// export type AlertRemarkDB = AlertRemark & {
//   phase: `parser-ready` | `notification-available` | `notification-unavailable`;
//   address: string;
//   isCouncilMember?: boolean;
//   isTCMember?: boolean;
// };

// export type BlockDataDB = {
//   blockHash: string,
//   blockNumber: number,
//   alertRemarks: Array<AlertRemarkDB>;
//   phase: `parser-ready` | `notification-available` | `notification-unavailable`;
// };
